# AI 指令操作手冊 — 複製貼上就能用

> **使用方式：** 把下方灰色框框裡的指令，直接複製貼上到 Gemini CLI / ChatGPT / Colab AI 面板
> **不需要寫任何程式碼**，AI 會自動幫你處理
> **每個指令都附有「預期結果」**，讓你確認 AI 做對了

---

## 使用前準備

### Gemini CLI（本機操作）
```
cd 到 sample_data 資料夾
gemini
```

### Colab AI 面板（雲端操作）
1. 上傳 sample_data 裡的 Excel 檔案到 Colab
2. 點右上角 Gemini 圖示開啟 AI 面板
3. 把指令貼進去

### ChatGPT（生成程式碼後貼到 Colab 執行）
1. 把指令貼進 ChatGPT
2. ChatGPT 會給你 Python 程式碼
3. 複製程式碼到 Colab 執行

---

# 第一組指令：銷售資料分析（銷售資料.xlsx）

## 指令 1：看看這份資料有什麼

```
請讀取 銷售資料.xlsx，告訴我：
1. 這份資料有幾筆、有哪些欄位
2. 每個欄位的資料範例（前 3 筆）
3. 金額欄位的最小值、最大值、平均值
```

**預期結果：**
- 200 筆資料
- 7 個欄位：日期、業務員、產品類別、客戶名稱、數量、單價、金額
- 金額範圍約 250 ~ 250,000

---

## 指令 2：誰賣最多？

```
請讀取 銷售資料.xlsx，
計算每位業務員的銷售總金額，
由高到低排列，用表格顯示。
```

**預期結果：**

| 排名 | 業務員 | 銷售總額 |
|------|--------|----------|
| 1 | 張志豪 | 2,532,700 |
| 2 | 李美玲 | 2,184,700 |
| 3 | 王小明 | 1,798,200 |
| 4 | 林建宏 | 1,298,800 |
| 5 | 陳雅婷 | 1,053,050 |
| 6 | 黃淑芬 | 850,050 |

---

## 指令 3：哪類產品最暢銷？

```
請讀取 銷售資料.xlsx，
計算每個產品類別的銷售總金額和占比（百分比），
由高到低排列，用表格顯示。
```

**預期結果：**

| 產品類別 | 銷售總額 | 占比 |
|---------|----------|------|
| 生活用品 | 3,235,450 | 約 33% |
| 辦公用品 | 2,804,950 | 約 29% |
| 電子產品 | 2,123,650 | 約 22% |
| 食品飲料 | 1,553,450 | 約 16% |

---

## 指令 4：幫我做樞紐分析表

```
請讀取 銷售資料.xlsx，
做一個樞紐分析表：
- 列（row）：業務員
- 欄（column）：產品類別
- 值：銷售金額加總
- 最後加上每行和每列的合計

用表格顯示，金額用千分位格式。
```

**預期結果：**
一張 6 人 × 4 類產品的交叉表，右邊有合計，底部有合計，全部總計約 9,717,500。

---

## 指令 5：篩選特定條件

```
請讀取 銷售資料.xlsx，
找出同時符合以下條件的訂單：
- 業務員是「王小明」
- 產品類別是「電子產品」

列出所有符合的訂單，並計算總金額。
```

**預期結果：**
會列出王小明賣電子產品的所有訂單（筆數視資料而定），以及這些訂單的金額合計。

---

## 指令 6：生成新的統計報表 Excel

```
請讀取 銷售資料.xlsx，幫我產生一個新檔案叫「銷售統計報表.xlsx」，裡面包含 3 個工作表：

工作表 1「業務員排行」：每位業務員的訂單數、總金額、平均金額，由總金額高到低排列
工作表 2「產品分析」：每個產品類別的訂單數、總金額、占比
工作表 3「月度趨勢」：每個月的訂單數和總金額

金額都要有千分位格式。
```

**預期結果：**
生成一個 `銷售統計報表.xlsx`，打開後有 3 個工作表，數字與上面的指令結果一致。

---

# 第二組指令：成績處理（成績單.xlsx）

## 指令 7：幫學生打等第

```
請讀取 成績單.xlsx，
依照以下規則幫每位學生標記等第：
- 90 分以上：A
- 80-89 分：B
- 70-79 分：C
- 60-69 分：D
- 60 分以下：F

然後告訴我：
1. 各等第各有幾人
2. 全班平均分數
3. 最高分和最低分是誰

把加上等第的結果存成「成績單_含等第.xlsx」。
```

**預期結果：**
- 等第分布：A 有 6 人、B 有 6 人、C 有 5 人、D 有 3 人、F 有 10 人
- 全班平均約 69.2 分
- 最高分 95 分、最低分 36 分

---

# 第三組指令：資料清洗（原始資料.xlsx）

## 指令 8：幫我清理這份髒資料

```
請讀取 原始資料.xlsx，這份資料很亂，請幫我：

1. 先告訴我有什麼問題（重複、空值、格式不一致）
2. 移除完全重複的資料
3. 移除「姓名」或「金額」是空白的資料
4. 姓名欄位去掉前後空白
5. 金額欄位去掉文字（如「NTD」「元」），只保留數字
6. 電話欄位統一格式為 09XX-XXX-XXX
7. 把清理後的資料存成「清理後資料.xlsx」

每一步都告訴我處理了幾筆。
```

**預期結果：**
- 原始 68 筆
- 發現約 7 筆重複
- 發現約 5 筆姓名空值、3 筆金額空值
- 清理後約 53-56 筆乾淨資料
- 所有姓名無前後空白、金額都是純數字、電話格式統一

---

# 第四組指令：庫存比對（上月庫存.xlsx + 本月庫存.xlsx）

## 指令 9：比對兩個月的庫存差異

```
我有兩個 Excel 檔案：
- 上月庫存.xlsx（上個月的庫存）
- 本月庫存.xlsx（這個月的庫存）

兩個都有「產品編號」「產品名稱」「庫存量」欄位。

請比對差異，告訴我：
1. 庫存增加的產品（列出產品名、增加多少）
2. 庫存減少的產品（列出產品名、減少多少）
3. 本月新增的產品（上月沒有、本月有的）
4. 本月消失的產品（上月有、本月沒有的）

用表格顯示，並存成「庫存異動報告.xlsx」。
```

**預期結果：**
- 新增 1 項：環保紙杯（SKU013），庫存 210
- 消失 1 項：膠帶（SKU004），原庫存 89
- 其他 11 項各有增減，例如：
  - A4影印紙：203 → 337（增加 134）
  - 原子筆(藍)：297 → 251（減少 46）
  - 計算機：354 → 479（增加 125）

---

# 第五組指令：合併多部門（業務部~管理部.xlsx）

## 指令 10：合併 5 個部門的資料

```
目前資料夾有 5 個 Excel 檔案：
業務部.xlsx、行銷部.xlsx、客服部.xlsx、研發部.xlsx、管理部.xlsx

每個檔案都有：日期、業務員、產品類別、金額 這 4 個欄位。

請幫我：
1. 讀取全部 5 個檔案
2. 合併成一個表，並加上「部門」欄位標記來源
3. 統計每個部門的訂單數和總金額
4. 找出全公司銷售額最高的前 5 名業務員
5. 把合併後的完整資料存成「全公司銷售資料.xlsx」
```

**預期結果：**

| 部門 | 訂單數 | 總金額 |
|------|--------|--------|
| 業務部 | 50 | 2,854,469 |
| 行銷部 | 45 | 2,129,732 |
| 管理部 | 40 | 2,029,013 |
| 客服部 | 32 | 1,754,261 |
| 研發部 | 33 | 1,464,380 |
| **合計** | **200** | **10,231,855** |

---

# 第六組指令：一步完成月報（綜合）

## 指令 11：一個指令做完月報

```
我有 5 個部門的銷售 Excel：業務部.xlsx、行銷部.xlsx、客服部.xlsx、研發部.xlsx、管理部.xlsx。
每個檔案有：日期、業務員、產品類別、金額。

請一次幫我完成以下所有事情：

1. 合併 5 個檔案，加上「部門」欄位
2. 產生「月報.xlsx」，包含以下工作表：
   - 「部門摘要」：每個部門的訂單數、總金額、平均金額，由高到低
   - 「業務員排行」：全公司業務員銷售排行 Top 10
   - 「產品分析」：各產品類別的銷售統計
   - 「完整明細」：所有合併後的原始資料
3. 金額都要有千分位格式
4. 全公司 5 個部門共 200 筆資料，總金額約 1,023 萬
```

**預期結果：**
生成一個 `月報.xlsx`，4 個工作表，數字與指令 10 的結果一致。

---

# 第七組指令：用 VLOOKUP 概念合併（訂單.xlsx + 產品.xlsx）

## 指令 12：幫訂單查出產品名稱和單價

```
我有兩個 Excel：
- 訂單.xlsx：有「訂單編號、日期、客戶名稱、產品編號、數量」
- 產品.xlsx：有「產品編號、產品名稱、單價」

請幫我：
1. 用「產品編號」把兩份資料合併
2. 算出每筆訂單的「金額 = 數量 × 單價」
3. 列出金額最高的前 5 筆訂單
4. 統計每個客戶的採購總金額
5. 存成「訂單明細.xlsx」
```

**預期結果：**
- 50 筆訂單全部查到對應的產品名稱和單價
- 每筆都有計算出的金額
- 產品包含：筆記型電腦（28,000）、27吋螢幕（8,900）等

---

# 第八組指令：圖表生成

## 指令 13：幫我畫銷售圖表

```
請讀取 銷售資料.xlsx，幫我畫以下圖表並存成 PNG 圖片：

1. 長條圖：各業務員銷售總額（由高到低）
   → 存成「業務員銷售長條圖.png」

2. 圓餅圖：各產品類別銷售占比
   → 存成「產品占比圓餅圖.png」

圖表標題用繁體中文，金額顯示千分位。
```

**預期結果：**
- 長條圖：張志豪最高（約 253 萬），黃淑芬最低（約 85 萬）
- 圓餅圖：生活用品約 33%、辦公用品約 29%、電子產品約 22%、食品飲料約 16%

---

# 第九組指令：每月資料合併

## 指令 14：合併 12 個月的工作表

```
請讀取 每月資料.xlsx，這個檔案有 12 個工作表（1月、2月...12月），
每個工作表有：日期、項目、金額。

請幫我：
1. 把 12 個工作表合併成一張表，加上「來源月份」欄位
2. 統計每個月的支出總金額
3. 找出花費最多的項目 Top 5
4. 存成「年度支出彙總.xlsx」
```

**預期結果：**
- 合併後約 200-250 筆資料
- 12 個月各有金額小計
- 項目包含：辦公桌椅、影印紙、軟體授權等

---

# 確保結果一致的秘訣

> **為什麼不同 AI 模型會給出一樣的結果？**
>
> 因為我們的指令夠具體：
> 1. **指定了檔案名稱** — AI 不需要猜
> 2. **指定了欄位名稱** — AI 知道要用哪個欄位
> 3. **指定了計算方式** — 加總、平均、排序都明確說了
> 4. **指定了輸出格式** — 表格、千分位、由高到低
>
> 只要資料一樣、指令一樣，不管用 ChatGPT、Gemini、Claude，
> 算出來的數字就是一樣的。
>
> **這就是 Prompt 工程的核心：描述越具體，結果越一致。**

---

# 快速查找

| 我想做什麼 | 用哪個指令 |
|-----------|-----------|
| 看資料有什麼 | 指令 1 |
| 統計排名 | 指令 2、3 |
| 樞紐分析 | 指令 4 |
| 篩選條件 | 指令 5 |
| 生成報表 Excel | 指令 6 |
| 評分打等第 | 指令 7 |
| 清理髒資料 | 指令 8 |
| 比對兩份資料 | 指令 9 |
| 合併多個檔案 | 指令 10、14 |
| 完整月報 | 指令 11 |
| 查找合併（VLOOKUP）| 指令 12 |
| 畫圖表 | 指令 13 |
